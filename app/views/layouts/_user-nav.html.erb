

<div class="header">
    <div class="container">
        <nav class="navbar navbar-main">

            <div class="logo">
                <h4>GooCD</h4>
            </div>
                <div class="slidemenu">
                    <ul class="nav navbar-nav navbar-left ">
                        <li><%= link_to '商品一覧', "/products", class: "glyphicon glyphicon-headphones headphones" %></li>
                        <li>
                          <div class="form-inline">
                            <div class="input-group">
                                <%= search_form_for @q, url: products_path do |f| %>
                                <%= f.search_field :name_or_artist_name_or_songs_name_cont, class:"form-control search-form",style:"width:300px",placeholder:"商品名・アーティスト名・曲名で検索" %>
                                <span class="input-group-btn">
                                    <%= f.submit "検索", class:"btn btn-default" %>
                                </span>    
                            　　 <% end %>
                            </div>
                          </div>
                        </li>
                    </ul> 

                    <ul class="nav navbar-nav navbar-right">
                        <% if user_signed_in? %>
                            <li><%= link_to 'カートへ', "/#", class: "glyphicon glyphicon-shopping-cart cart" %><li>
                            <li><%= link_to ' マイページ', "/users/#{current_user.id}", class: "glyphicon glyphicon-user users" %></li>
                            <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out logout" %></li>
                        <% else %>
                            <li><%= link_to 'ログイン', "/users/sign_in", class: "glyphicon glyphicon-log-in sign_in" %></li>
                            <li><%= link_to '会員登録', "/users/sign_up", class: "glyphicon glyphicon-edit sign_up" %><li>
                        <% end %>

                    </ul>
                </div>

        </nav>
    </div>
</div>
